# Ejemplo de configuración de base de datos para desarrollo
DATABASE_URL="postgresql://usuario:contraseña@localhost:5432/tu_basededatos"

# Puerto en el que se ejecutará el servidor
PORT=

# Configuración de SMTP para envío de correos
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# URL del frontend para redireccionamientos
FRONTEND_URL=